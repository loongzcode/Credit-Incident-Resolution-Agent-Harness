# This shared-process env file is retired. Do not copy one secret set into all services.
# Use api/admin/agent/orchestration/recovery/embedding.env.example separately.
# Supply their paths as API_ENV_FILE, ADMIN_ENV_FILE, AGENT_ENV_FILE,
# ORCHESTRATION_ENV_FILE, RECOVERY_ENV_FILE, EMBEDDING_ENV_FILE.
# Run: python -m scripts.deploy_production --check-only
